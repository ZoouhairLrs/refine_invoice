{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    },
    "noEmitOnError": false, // Allows code to compile despite errors
    "skipLibCheck": true,   // Skips type checking for libraries in node_modules
    "strict": false,        // Disables strict type checks
  }
}
